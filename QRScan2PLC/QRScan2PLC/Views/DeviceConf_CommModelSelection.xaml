<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="QRScan2PLC.Views.DeviceConf_CommModelSelection" BackgroundColor="#373737">

    <StackLayout Orientation="Vertical" Margin="20,50,20,20">
        <StackLayout Orientation="Vertical">
            <Label TextColor="White" FontSize="Small" HorizontalTextAlignment="Center" Text="SELECT DEVICE TO COMMUNICATE MODEL"/>
            <Frame HeightRequest="40" WidthRequest="260" CornerRadius="10" HorizontalOptions="Center" VerticalOptions="Center" Margin="0" Padding="0">
                <Picker ItemsSource="{Binding ModelSelectionList,Mode=TwoWay}"  SelectedIndex="{Binding DeviceModelSelected}" BackgroundColor="White" TextColor="Black">
                    <Picker.Behaviors>
                        <prism:EventToCommandBehavior EventName="SelectedIndexChanged"
                                          Command="{Binding ModelSelectCmd}"/>
                    </Picker.Behaviors>
                </Picker>
            </Frame>
        </StackLayout>
        <StackLayout Margin="0,10,0,0"  Orientation="Vertical">
            <Button Text="BACK" Command="{Binding BackCmd}"></Button>
        </StackLayout>
    </StackLayout>
</ContentPage>